-- Custom User Configuration
-- This file is for personal settings that won't be committed to git

local M = {}

function M.setup()
  -- Custom colors (optional)
  -- vim.api.nvim_set_hl(0, "Normal", { bg = "#1a1a2e" })
  
  -- Personal keymaps (optional)
  -- local keymap = vim.keymap
  -- keymap.set('n', '<leader>h', ':echo "Hello!"<CR>', { desc = 'Custom command' })
  
  -- Project-specific settings (optional)
  -- if vim.fn.getcwd():match("my-project") then
  --   vim.opt.tabstop = 2
  -- end
end

return M